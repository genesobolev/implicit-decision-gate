"""Baseline contract for workspace export creation."""


def create_export(role: str, export_jobs: list[str]) -> int:
    """Return the result; export_jobs contains the workspace's existing jobs."""
    raise NotImplementedError
